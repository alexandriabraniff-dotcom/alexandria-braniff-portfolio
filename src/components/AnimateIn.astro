---
// AnimateIn — animations disabled, renders content immediately
interface Props {
  delay?: number;
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={className}>
  <slot />
</div>
